const models = {
    productModel : require('./mongo/productModel'),
    userModel: require('./mongo/userModel'),
    favoriteCharacterModel: require('./mongo/favoriteCharacterModel')
}

module.exports = models